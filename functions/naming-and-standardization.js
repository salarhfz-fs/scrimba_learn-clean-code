// Descriptive Names
function addUserToDB(user) {
    const userData = { ...user, type: 'Admin' };

    // adds user to DB
}

// Standardization of Naming - camelCase
function getAdminById(id) {
    //  gets admin
}

function getUserById(id) {
    // gets user    
}